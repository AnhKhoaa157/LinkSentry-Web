# Minimal dependencies for ml/. All are local, offline libraries -- none
# perform network I/O as part of feature extraction, training, or inference.
#
# numpy, pandas    -- feature matrix / dataset handling
# scikit-learn     -- baseline models, group-aware splitting, metrics
# joblib           -- model artifact persistence (already a scikit-learn
#                      dependency; pinned directly since we import it too)
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
joblib>=1.3
