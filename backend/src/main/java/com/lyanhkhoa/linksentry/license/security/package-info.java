/**
 * Device credential generation/hashing and the security principal installed for a currently licensed
 * device. See {@code common.security.DeviceAuthenticationFilter}, which is the only place a raw device
 * credential is read off a request.
 */
package com.lyanhkhoa.linksentry.license.security;
