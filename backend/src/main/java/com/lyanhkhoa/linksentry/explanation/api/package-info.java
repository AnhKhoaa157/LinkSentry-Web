/**
 * HTTP boundary for {@code POST /api/v1/scans/{scanId}/explanation}.
 *
 * <p>Authenticated and owner-scoped only — see {@code common.security.SecurityConfig}
 * for the route requirement and {@code common.ratelimit} for this route's own
 * independent, strict token bucket.
 */
package com.lyanhkhoa.linksentry.explanation.api;
