# The browser extension calls the deployed API directly. This is intentionally
# separate from the web app's local-development default in .env.example.
VITE_API_BASE_URL=https://linksentry-web.onrender.com
